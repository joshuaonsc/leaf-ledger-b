# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# App data: personal match logs and exports.
# These are your own records; don't publish them by accident.
# To intentionally share a log, force-add it: git add -f <file>
leafledger*.json
*.csv

# Claude Code working directory: local settings and preview helpers, not part of the app.
.claude/

# Node modules (CI-only axe runner; never committed)
node_modules/
